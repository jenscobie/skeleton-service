project.buildDir = 'target'

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'io.dropwizard:dropwizard-core:0.8.1'
    }
}

apply plugin: 'idea'

repositories {
    mavenCentral()
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.3'
}